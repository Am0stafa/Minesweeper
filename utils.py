import setting

# we will have a function to calculate how much is x percent of the entire window

def hightPrct(num):
    return (setting.HEIGHT/100) * num
def widthPrct(num):
    return (setting.WIDTH/100) * num
    
#! 3ayzeen x amount of percent form the hight or width